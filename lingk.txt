https://nelsonyikwa.github.io/Portofolio/
/*************************************************************
  Blynk is a platform with iOS and Android apps to control
  ESP32, Arduino, Raspberry Pi and the likes over the Internet.
  You can easily build mobile and web interfaces for any
  projects by simply dragging and dropping widgets.

    Downloads, docs, tutorials: https://www.blynk.io
    Sketch generator:           https://examples.blynk.cc
    Blynk community:            https://community.blynk.cc
    Follow us:                  https://www.fb.com/blynkapp
                                https://twitter.com/blynk_app

  Blynk library is licensed under MIT license
 *************************************************************
  Blynk.Edgent implements:
  - Blynk.Inject - Dynamic WiFi credentials provisioning
  - Blynk.Air    - Over The Air firmware updates
  - Device state indication using a physical LED
  - Credentials reset using a physical Button
 *************************************************************/

/* Fill in information from your Blynk Template here */
/* Read more: https://bit.ly/BlynkInject */
#define BLYNK_TEMPLATE_ID           "TMPL6rGAzFIzO"
#define BLYNK_TEMPLATE_NAME         "Quikstart Template"

#define BLYNK_FIRMWARE_VERSION        "0.1.0"

#define BLYNK_PRINT Serial
//#define BLYNK_DEBUG

#define APP_DEBUG

// Uncomment your board, or configure a custom board in Settings.h
//#define USE_SPARKFUN_BLYNK_BOARD
#define USE_NODE_MCU_BOARD
//#define USE_WITTY_CLOUD_BOARD
//#define USE_WEMOS_D1_MINI

#include "BlynkEdgent.h"

//fungsi untuk virtual Pin v0
BLYNK_WRITE(v0)
{
  if (param.asint() == HIGH)
  {
    digitalWrite(D0, HIGH); //nyalan perangkat di D0
  }
  else
  {
    digitalWrite(D0, Low); // Matikan perangkat di D0
  }
}

//fungsi untuk virtual Pin v1
BLYNK_WRITE(v1)
{
  if (param.asint() == HIGH)
  {
    digitalWrite(D1, HIGH); //nyalan perangkat di D1
  }
  else
  {
    digitalWrite(D1, Low); // Matikan perangkat di D1
  }
}

//fungsi untuk virtual Pin v2
BLYNK_WRITE(v2)
{
  if (param.asint() == HIGH)
  {
    digitalWrite(D2, HIGH); //nyalan perangkat di D2
  }
  else
  {
    digitalWrite(D2, Low); // Matikan perangkat di D2
  }
}

void setup()
{
  Serial.begin(115200);
  delay(100);

// Atur pin Fisik Sebagai Output
pinmode(D0, OUTPUT);
pinMode(D1, OUTPUT);
pinMode(D2, OUTPUT);

// Instalasasi Blynk
  BlynkEdgent.begin();
}


void loop() {
  BlynkEdgent.run();
}

